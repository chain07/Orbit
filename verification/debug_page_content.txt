<!DOCTYPE html><html lang="en"><head>
    <script type="module">import { injectIntoGlobalHook } from "/@react-refresh";
injectIntoGlobalHook(window);
window.$RefreshReg$ = () => {};
window.$RefreshSig$ = () => (type) => type;</script>

    <script type="module" src="/@vite/client"></script>

    <meta charset="UTF-8">
    <link rel="icon" type="image/svg+xml" href="/icons/icon-72x72.png">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">

    <meta name="theme-color" content="#F2F2F7">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="ORBIT">

    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">
    <link rel="manifest" href="/manifest.json">

    <!-- Liquid Native Typography -->
    <link rel="preload" href="/fonts/Geist-Variable.woff2" as="font" type="font/woff2" crossorigin="">
    <style>
      @font-face {
        font-family: 'Geist';
        src: url('/fonts/Geist-Variable.woff2') format('woff2');
        font-weight: 100 900;
        font-display: swap;
        size-adjust: 100%;
      }
    </style>

    <title>ORBIT - Life OS</title>
  <style type="text/css" data-vite-dev-id="/app/src/styles/tokens.css">/* src/styles/tokens.css */

/* =========================
   ORBIT Liquid Native — Design Tokens
   Source of Truth: docs/design-system.md
   ========================= */

:root {
  /* ---------- Liquid Glass Tokens ---------- */
  /* Defined as per "Liquid Glass" Material Spec */
  --glass-bg: rgba(255, 255, 255, 0.75);
  --glass-border: rgba(0, 0, 0, 0.1);

  /* ---------- Semantic Signals (Instrument Panel) ---------- */
  /* Negative (Error/Critical) - System Red */
  --red: #FF3B30;
  /* Notice (Warning/Pending) - System Orange */
  --orange: #FF9500;
  /* Positive (Success/Complete) - System Green */
  --green: #34C759;
  /* Informative (Active/Neutral) - Orbit Blue */
  --blue: #007AFF;

  /* ---------- Base Palette (Monochrome) ---------- */
  --bg-color: #F2F2F7; /* System Gray 6 */
  --card-bg: #FFFFFF;
  --text-primary: #000000; /* Zinc 900 equivalent */
  --text-secondary: #8E8E93; /* Zinc 500 equivalent */

  /* ---------- Typography (Geist) ---------- */
  /* L-09: Standardized Font Family */
  --font-system: "Geist", -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", sans-serif;

  /* ---------- Geometry (Inset Grouped) ---------- */
  --radius-card: 20px;
  --radius-pill: 35px;
  --border-width: 0.5px; /* High-density sub-pixel */

  /* ---------- Layout ---------- */
  --safe-bottom: env(safe-area-inset-bottom);
  --safe-top: env(safe-area-inset-top);

  /* ---------- Shadows ---------- */
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.02);
  --shadow-glass: 0 10px 40px rgba(0, 0, 0, 0.15);

  /* ---------- Additional Legacy Support (to be deprecated or mapped) ---------- */
  --separator: #C6C6C8;
  --grid-line: #E5E5EA;
  --yellow: #FFCC00;
  --purple: #AF52DE;
  --indigo: #5856D6;
  --pink: #FF2D55;
  --cyan: #38C7F4;
  --sky: #5AC8FA;
  --gray: #8E8E93;
}

/* =========================
   Dark Mode
   ========================= */

@media (prefers-color-scheme: dark) {
  :root {
    --bg-color: #000000;
    --card-bg: #1C1C1E;

    /* Dark Mode Glass Adaptation */
    --glass-bg: rgba(28, 28, 30, 0.75);
    --glass-border: rgba(255, 255, 255, 0.1);

    --text-primary: #FFFFFF;
    --text-secondary: #98989D;

    --separator: #38383A;
    --grid-line: #38383A;

    --shadow-card: 0 2px 10px rgba(0, 0, 0, 0.35);
    --shadow-glass: 0 10px 40px rgba(0, 0, 0, 0.5);
  }
}
</style><style type="text/css" data-vite-dev-id="/app/src/styles/motion.css">/* src/styles/motion.css */

/* -----------------------------
   Global Motion Defaults
-------------------------------- */
:root {
  /* Standard iOS easing */
  --ease-ios: cubic-bezier(0.25, 1, 0.5, 1);
  /* Spring for bouncy elements (Tabs) */
  --ease-spring: cubic-bezier(0.25, 1, 0.5, 1.2);
  /* "Snappy" curve from HTML Segmented Control */
  --ease-snappy: cubic-bezier(0.2, 0.8, 0.2, 1);

  --dur-fast: 0.2s;
  --dur-med: 0.4s;
  --dur-slow: 0.6s;
}

/* -----------------------------
   Page & View Transitions
-------------------------------- */
.fade-in {
  animation: fadeIn var(--dur-med) ease-out forwards;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.slide-in {
  animation: slideIn var(--dur-med) ease-out forwards;
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* -----------------------------
   Cards
-------------------------------- */
.card {
  transition:
    transform var(--dur-fast) var(--ease-standard),
    box-shadow var(--dur-fast) var(--ease-standard);
}

.card:active {
  transform: scale(0.98);
}

@media (hover: hover) {
  .card:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 14px rgba(0,0,0,0.06);
  }
}

/* -----------------------------
   Buttons & Micro-Interactions
-------------------------------- */
.btn-ios, button {
  transition:
    background-color 0.2s ease,
    transform 0.1s ease,
    opacity 0.2s ease;
}

.btn-ios:active, button:active {
  transform: scale(0.96); /* Tighter click feel */
  opacity: 0.8;
}

/* -----------------------------
   Segmented Control
-------------------------------- */
.seg-indicator {
  /* Matches HTML Reference exactly */
  transition: transform 0.3s var(--ease-snappy), width 0.3s var(--ease-snappy);
}

.seg-btn {
  transition: color 0.2s ease;
}

/* -----------------------------
   Bottom Tab Bar
-------------------------------- */
.tab-item svg {
  transition: fill 0.3s ease, transform 0.2s var(--ease-spring);
}

.tab-item:active svg {
    transform: scale(0.9);
}

.tab-indicator {
  transition:
    transform 0.4s var(--ease-spring),
    width 0.3s ease;
}

/* -----------------------------
   Modal
-------------------------------- */
.modal-overlay {
  transition: opacity 0.3s ease;
}

.modal-card {
  transition: transform 0.3s var(--ease-ios);
}

/* -----------------------------
   Reduced Motion
-------------------------------- */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.001ms !important;
    transition-duration: 0.001ms !important;
  }
}
</style><style type="text/css" data-vite-dev-id="/app/src/styles/layout.css">/* src/styles/layout.css */
/* Extracted from reference/dispatch.html for Native iOS fidelity */

:root {
  --safe-area-top: env(safe-area-inset-top);
  --safe-area-bottom: env(safe-area-inset-bottom);
  --nav-height: 60px;
}

/* Global Padding Strategy */
.layout-padding {
  padding-bottom: calc(var(--safe-area-bottom) + var(--nav-height) + 20px);
}

/* Standard Content Alignment (Matches Header) */
.layout-content {
  padding-left: 20px;
  padding-right: 20px;
}

/* Safe Area Utilities */
.safe-pt {
  padding-top: max(20px, var(--safe-area-top));
}

.safe-mt {
  margin-top: max(10px, var(--safe-area-top));
}

/* Universal Header Container */
.view-header-stack {
  display: flex;
  flex-direction: column;
  /* Safe area top padding + small buffer */
  padding-top: calc(env(safe-area-inset-top) + 10px);
  padding-bottom: 24px;
  padding-left: 20px;
  padding-right: 20px;
  /* Strict gap between Title and Subtitle/Controls */
  gap: 4px;
}

/* Force override browser defaults */
.view-header-stack h1 {
  margin: 0;
  line-height: 1;
}

.view-header-stack p {
  margin-top: 4px;
  margin-bottom: 16px; /* Space before toggle */
}

/* Toggle Wrapper for consistency */
.system-toggle-wrapper {
  width: 100%;
}

/* Fixed Bottom Navigation Container */
.tab-bar-container {
  position: fixed;
  bottom: calc(30px + var(--safe-area-bottom));
  left: 50%;
  transform: translateX(-50%);
  z-index: 100; /* High but below Modals (z-50 usually handled by fixed overlay) */
  width: auto;
  display: flex;
  justify-content: center;
}

/* Glass Pill Styling */
.tab-bar {
  /* Liquid Glass Token Implementation */
  background: var(--glass-bg);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);

  border-radius: 35px !important;
  padding: 6px !important;
  display: flex;
  gap: 4px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.15);
  border: 1px solid var(--glass-border);
  position: relative;
}

/* Tab Item */
.tab-item {
  padding: 12px 20px;
  position: relative;
  z-index: 2;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  /* Ensure consistent height/width behavior if needed */
  min-width: 44px;
}

.tab-item svg {
  width: 24px;
  height: 24px;
  color: var(--text-secondary);
  transition: color 0.3s ease;
  /* Dispatch uses fill, but our Icons use stroke/currentColor */
  stroke-width: 2px;
}

.tab-item.active svg {
  color: var(--blue);
  filter: drop-shadow(0 0 8px rgba(0, 122, 255, 0.4));
}

/* Sliding Active Indicator */
.tab-indicator {
  position: absolute;
  top: 6px;
  left: 6px;
  height: calc(100% - 12px);
  background: rgba(0,0,0,0.08); /* Light mode */
  border-radius: 30px;
  z-index: 1;
  transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1.2), width 0.3s ease;
  pointer-events: none;
}

@media (prefers-color-scheme: dark) {
  .tab-indicator {
    background: rgba(255,255,255,0.15); /* Dark mode */
  }
}
</style><style type="text/css" data-vite-dev-id="/app/src/styles/buttons.css">/* src/styles/buttons.css */

/* --- TOKENS (Scoped Mapping) --- */
:root {
    --radius-btn: 14px;
    --scale-press: 0.95;

    /* Mapping to existing Orbit system tokens */
    --color-primary: var(--blue);
    --color-text-main: var(--text-primary);
    --color-negative: var(--red);
}

/* --- COMPONENT STYLES --- */
.btn-orbit {
    position: relative;
    width: 100%;
    height: 48px; /* Fixed Height Compliance */
    border: none;
    background: transparent;
    cursor: pointer;
    touch-action: manipulation;
    padding: 0; /* Reset default padding */
    -webkit-tap-highlight-color: transparent;
}

.btn-core {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
    border-radius: var(--radius-btn);
    gap: 8px;
    font-weight: 600;
    /* Physics: Release (Spring Back) */
    transition: transform 0.5s cubic-bezier(0.19, 1, 0.22, 1), background-color 0.2s ease;
}

/* PRESSED STATE */
.btn-orbit.is-pressed .btn-core {
    transform: scale(var(--scale-press));
    /* Physics: Press (Instant Squish) */
    transition: transform 0.1s cubic-bezier(0, 0, 0.2, 1);
}

/* VARIANTS */
.btn-primary .btn-core {
    background: var(--color-primary);
    color: white;
    box-shadow: inset 0 1px 0.5px rgba(255,255,255,0.25);
}

.btn-secondary .btn-core {
    background: rgba(0, 0, 0, 0.08);
    color: var(--color-text-main);
}

.btn-destructive .btn-core {
    background: rgba(255, 59, 48, 0.1);
    color: var(--color-negative);
}

/* Dark Mode Adjustments for Secondary/Destructive */
@media (prefers-color-scheme: dark) {
    .btn-secondary .btn-core {
        background: rgba(255, 255, 255, 0.1);
    }
}
</style><style type="text/css" data-vite-dev-id="/app/src/styles/metric_builder.css">/* src/styles/metric_builder.css */

/* --- Modal Header --- */
.metric-modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  border-bottom: 1px solid var(--separator);
  background-color: var(--bg-color);
  position: sticky;
  top: 0;
  z-index: 10;
  /* Match Main Card Radius */
  border-top-left-radius: var(--radius-card);
  border-top-right-radius: var(--radius-card);
}

.metric-modal-title {
  font-size: 20px;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
}

.btn-cancel-text {
  color: #8E8E93; /* Neutral gray */
  font-weight: 500;
  font-size: 16px;
  background: none;
  border: none;
  padding: 8px 16px;
  cursor: pointer;
  transition: color 0.2s;
}

.btn-cancel-text:hover {
  color: var(--text-primary);
}

.btn-cancel-text:active {
  opacity: 0.7;
}

/* --- Form Grid & Layout --- */
.form-row {
  display: flex;
  gap: 16px;
  width: 100%;
}

.form-group {
  flex: 1;
  display: flex;
  flex-direction: column;
}

/* Vertical alignment for Color Input to match Select */
.form-group-color {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-end; /* Aligns input to bottom */
}

/* --- Inputs Polish --- */
.input-standard {
  height: 44px; /* Standardize height */
  width: 100%;
  padding: 0 12px;
  border-radius: 10px; /* Consistent radius */
  border: 1px solid var(--separator);
  background-color: transparent;
  color: var(--text-primary);
  font-size: 16px; /* Legible size */
  font-weight: 500;
  outline: none;
  appearance: none; /* Remove native UI for selects */
  transition: border-color 0.2s;
}

.input-standard:focus {
  border-color: var(--blue);
}

/* Special handling for Color Input to match height */
.input-color {
  height: 44px !important;
  width: 100%;
  padding: 2px 4px; /* Padding for inner color swatch */
  border-radius: 10px;
  border: 1px solid var(--separator);
  background-color: transparent;
  cursor: pointer;
}

/* --- Labels --- */
.label-standard {
  font-size: 11px;
  font-weight: 700;
  color: var(--text-secondary);
  text-transform: uppercase;
  margin-bottom: 8px; /* Consistent gap */
  letter-spacing: 0.5px;
}

/* --- Divider Section --- */
.section-divider {
  margin-top: 24px;
  margin-bottom: 8px; /* Tighter gap */
  padding-bottom: 8px;
  border-bottom: 1px solid rgba(0,0,0,0.05); /* Subtle line */
  font-size: 11px;
  font-weight: 700;
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* Dark mode adjust for divider */
@media (prefers-color-scheme: dark) {
  .section-divider {
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }
}

/* --- Content Wrapper --- */
.modal-content-scroll {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 16px; /* Vertical rhythm */
  overflow-y: auto;
  flex: 1;
}

.modal-footer {
  padding: 16px;
  border-top: 1px solid var(--separator);
  background-color: var(--bg-color);
  /* Match Main Card Radius */
  border-bottom-left-radius: var(--radius-card);
  border-bottom-right-radius: var(--radius-card);
}

.visualization-section {
  display: flex;
  flex-direction: column;
  gap: 0; /* Tight gap */
}

.widget-helper-text {
  font-size: 11px;
  color: var(--text-secondary);
  margin-top: 8px;
  line-height: 1.4;
}
</style><style type="text/css" data-vite-dev-id="/app/src/styles/index.css">/* src/styles/index.css */
/* src/styles/tokens.css */
/* =========================
   ORBIT Liquid Native — Design Tokens
   Source of Truth: docs/design-system.md
   ========================= */
:root {
  /* ---------- Liquid Glass Tokens ---------- */
  /* Defined as per "Liquid Glass" Material Spec */
  --glass-bg: rgba(255, 255, 255, 0.75);
  --glass-border: rgba(0, 0, 0, 0.1);

  /* ---------- Semantic Signals (Instrument Panel) ---------- */
  /* Negative (Error/Critical) - System Red */
  --red: #FF3B30;
  /* Notice (Warning/Pending) - System Orange */
  --orange: #FF9500;
  /* Positive (Success/Complete) - System Green */
  --green: #34C759;
  /* Informative (Active/Neutral) - Orbit Blue */
  --blue: #007AFF;

  /* ---------- Base Palette (Monochrome) ---------- */
  --bg-color: #F2F2F7; /* System Gray 6 */
  --card-bg: #FFFFFF;
  --text-primary: #000000; /* Zinc 900 equivalent */
  --text-secondary: #8E8E93; /* Zinc 500 equivalent */

  /* ---------- Typography (Geist) ---------- */
  /* L-09: Standardized Font Family */
  --font-system: "Geist", -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", sans-serif;

  /* ---------- Geometry (Inset Grouped) ---------- */
  --radius-card: 20px;
  --radius-pill: 35px;
  --border-width: 0.5px; /* High-density sub-pixel */

  /* ---------- Layout ---------- */
  --safe-bottom: env(safe-area-inset-bottom);
  --safe-top: env(safe-area-inset-top);

  /* ---------- Shadows ---------- */
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.02);
  --shadow-glass: 0 10px 40px rgba(0, 0, 0, 0.15);

  /* ---------- Additional Legacy Support (to be deprecated or mapped) ---------- */
  --separator: #C6C6C8;
  --grid-line: #E5E5EA;
  --yellow: #FFCC00;
  --purple: #AF52DE;
  --indigo: #5856D6;
  --pink: #FF2D55;
  --cyan: #38C7F4;
  --sky: #5AC8FA;
  --gray: #8E8E93;
}
/* =========================
   Dark Mode
   ========================= */
@media (prefers-color-scheme: dark) {
  :root {
    --bg-color: #000000;
    --card-bg: #1C1C1E;

    /* Dark Mode Glass Adaptation */
    --glass-bg: rgba(28, 28, 30, 0.75);
    --glass-border: rgba(255, 255, 255, 0.1);

    --text-primary: #FFFFFF;
    --text-secondary: #98989D;

    --separator: #38383A;
    --grid-line: #38383A;

    --shadow-card: 0 2px 10px rgba(0, 0, 0, 0.35);
    --shadow-glass: 0 10px 40px rgba(0, 0, 0, 0.5);
  }
}
/* src/styles/motion.css */
/* -----------------------------
   Global Motion Defaults
-------------------------------- */
:root {
  /* Standard iOS easing */
  --ease-ios: cubic-bezier(0.25, 1, 0.5, 1);
  /* Spring for bouncy elements (Tabs) */
  --ease-spring: cubic-bezier(0.25, 1, 0.5, 1.2);
  /* "Snappy" curve from HTML Segmented Control */
  --ease-snappy: cubic-bezier(0.2, 0.8, 0.2, 1);

  --dur-fast: 0.2s;
  --dur-med: 0.4s;
  --dur-slow: 0.6s;
}
/* -----------------------------
   Page & View Transitions
-------------------------------- */
.fade-in {
  animation: fadeIn var(--dur-med) ease-out forwards;
}
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
.slide-in {
  animation: slideIn var(--dur-med) ease-out forwards;
}
@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
/* -----------------------------
   Cards
-------------------------------- */
.card {
  transition:
    transform var(--dur-fast) var(--ease-standard),
    box-shadow var(--dur-fast) var(--ease-standard);
}
.card:active {
  transform: scale(0.98);
}
@media (hover: hover) {
  .card:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 14px rgba(0,0,0,0.06);
  }
}
/* -----------------------------
   Buttons & Micro-Interactions
-------------------------------- */
.btn-ios, button {
  transition:
    background-color 0.2s ease,
    transform 0.1s ease,
    opacity 0.2s ease;
}
.btn-ios:active, button:active {
  transform: scale(0.96); /* Tighter click feel */
  opacity: 0.8;
}
/* -----------------------------
   Segmented Control
-------------------------------- */
.seg-indicator {
  /* Matches HTML Reference exactly */
  transition: transform 0.3s var(--ease-snappy), width 0.3s var(--ease-snappy);
}
.seg-btn {
  transition: color 0.2s ease;
}
/* -----------------------------
   Bottom Tab Bar
-------------------------------- */
.tab-item svg {
  transition: fill 0.3s ease, transform 0.2s var(--ease-spring);
}
.tab-item:active svg {
    transform: scale(0.9);
}
.tab-indicator {
  transition:
    transform 0.4s var(--ease-spring),
    width 0.3s ease;
}
/* -----------------------------
   Modal
-------------------------------- */
.modal-overlay {
  transition: opacity 0.3s ease;
}
.modal-card {
  transition: transform 0.3s var(--ease-ios);
}
/* -----------------------------
   Reduced Motion
-------------------------------- */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.001ms !important;
    transition-duration: 0.001ms !important;
  }
}
/* src/styles/layout.css */
/* Extracted from reference/dispatch.html for Native iOS fidelity */
:root {
  --safe-area-top: env(safe-area-inset-top);
  --safe-area-bottom: env(safe-area-inset-bottom);
  --nav-height: 60px;
}
/* Global Padding Strategy */
.layout-padding {
  padding-bottom: calc(var(--safe-area-bottom) + var(--nav-height) + 20px);
}
/* Standard Content Alignment (Matches Header) */
.layout-content {
  padding-left: 20px;
  padding-right: 20px;
}
/* Safe Area Utilities */
.safe-pt {
  padding-top: max(20px, var(--safe-area-top));
}
.safe-mt {
  margin-top: max(10px, var(--safe-area-top));
}
/* Universal Header Container */
.view-header-stack {
  display: flex;
  flex-direction: column;
  /* Safe area top padding + small buffer */
  padding-top: calc(env(safe-area-inset-top) + 10px);
  padding-bottom: 24px;
  padding-left: 20px;
  padding-right: 20px;
  /* Strict gap between Title and Subtitle/Controls */
  gap: 4px;
}
/* Force override browser defaults */
.view-header-stack h1 {
  margin: 0;
  line-height: 1;
}
.view-header-stack p {
  margin-top: 4px;
  margin-bottom: 16px; /* Space before toggle */
}
/* Toggle Wrapper for consistency */
.system-toggle-wrapper {
  width: 100%;
}
/* Fixed Bottom Navigation Container */
.tab-bar-container {
  position: fixed;
  bottom: calc(30px + var(--safe-area-bottom));
  left: 50%;
  transform: translateX(-50%);
  z-index: 100; /* High but below Modals (z-50 usually handled by fixed overlay) */
  width: auto;
  display: flex;
  justify-content: center;
}
/* Glass Pill Styling */
.tab-bar {
  /* Liquid Glass Token Implementation */
  background: var(--glass-bg);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);

  border-radius: 35px !important;
  padding: 6px !important;
  display: flex;
  gap: 4px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.15);
  border: 1px solid var(--glass-border);
  position: relative;
}
/* Tab Item */
.tab-item {
  padding: 12px 20px;
  position: relative;
  z-index: 2;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  /* Ensure consistent height/width behavior if needed */
  min-width: 44px;
}
.tab-item svg {
  width: 24px;
  height: 24px;
  color: var(--text-secondary);
  transition: color 0.3s ease;
  /* Dispatch uses fill, but our Icons use stroke/currentColor */
  stroke-width: 2px;
}
.tab-item.active svg {
  color: var(--blue);
  filter: drop-shadow(0 0 8px rgba(0, 122, 255, 0.4));
}
/* Sliding Active Indicator */
.tab-indicator {
  position: absolute;
  top: 6px;
  left: 6px;
  height: calc(100% - 12px);
  background: rgba(0,0,0,0.08); /* Light mode */
  border-radius: 30px;
  z-index: 1;
  transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1.2), width 0.3s ease;
  pointer-events: none;
}
@media (prefers-color-scheme: dark) {
  .tab-indicator {
    background: rgba(255,255,255,0.15); /* Dark mode */
  }
}
/* src/styles/segmented.css */
.segmented-control-wrapper {
  display: flex;
  position: relative;
  background: rgba(118, 118, 128, 0.12);
  border-radius: 9px;
  padding: 2px;
  width: 100%;
  height: 32px;
  box-sizing: border-box;
  overflow: hidden; /* Ensure rounded corners clip */
}
.segmented-control-wrapper input[type="radio"] {
  display: none;
}
.segmented-control-wrapper label {
  flex: 1;
  z-index: 2;
  text-align: center;
  cursor: pointer;
  font-size: 13px;
  font-weight: 500;
  line-height: 28px; /* Match container height minus padding */
  color: var(--text-primary);
  transition: opacity 0.2s;
  user-select: none;
  /* Optional: Ensure text truncation */
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.segmented-control-wrapper .glider {
  position: absolute;
  top: 2px;
  bottom: 2px;
  left: 2px; /* Base offset */
  background: #FFF;
  border-radius: 7px;
  box-shadow: 0 3px 8px rgba(0,0,0,0.12);
  transition: transform 0.3s cubic-bezier(0.25, 1, 0.5, 1);
  z-index: 1;
  pointer-events: none; /* Let clicks pass to labels */
}
/* Dark Mode Glider */
@media (prefers-color-scheme: dark) {
  .segmented-control-wrapper .glider {
    background: #636366;
  }
}
/* Dynamic Widths based on item count */
.segmented-control-wrapper[data-count="2"] .glider { width: calc((100% - 4px) / 2); }
.segmented-control-wrapper[data-count="3"] .glider { width: calc((100% - 4px) / 3); }
.segmented-control-wrapper[data-count="4"] .glider { width: calc((100% - 4px) / 4); }
.segmented-control-wrapper[data-count="5"] .glider { width: calc((100% - 4px) / 5); }
/* Animation Logic using :has() */
.segmented-control-wrapper:has(input:nth-of-type(1):checked) .glider { transform: translateX(0%); }
.segmented-control-wrapper:has(input:nth-of-type(2):checked) .glider { transform: translateX(100%); }
.segmented-control-wrapper:has(input:nth-of-type(3):checked) .glider { transform: translateX(200%); }
.segmented-control-wrapper:has(input:nth-of-type(4):checked) .glider { transform: translateX(300%); }
.segmented-control-wrapper:has(input:nth-of-type(5):checked) .glider { transform: translateX(400%); }
/* src/styles/buttons.css */
/* --- TOKENS (Scoped Mapping) --- */
:root {
    --radius-btn: 14px;
    --scale-press: 0.95;

    /* Mapping to existing Orbit system tokens */
    --color-primary: var(--blue);
    --color-text-main: var(--text-primary);
    --color-negative: var(--red);
}
/* --- COMPONENT STYLES --- */
.btn-orbit {
    position: relative;
    width: 100%;
    height: 48px; /* Fixed Height Compliance */
    border: none;
    background: transparent;
    cursor: pointer;
    touch-action: manipulation;
    padding: 0; /* Reset default padding */
    -webkit-tap-highlight-color: transparent;
}
.btn-core {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
    border-radius: var(--radius-btn);
    gap: 8px;
    font-weight: 600;
    /* Physics: Release (Spring Back) */
    transition: transform 0.5s cubic-bezier(0.19, 1, 0.22, 1), background-color 0.2s ease;
}
/* PRESSED STATE */
.btn-orbit.is-pressed .btn-core {
    transform: scale(var(--scale-press));
    /* Physics: Press (Instant Squish) */
    transition: transform 0.1s cubic-bezier(0, 0, 0.2, 1);
}
/* VARIANTS */
.btn-primary .btn-core {
    background: var(--color-primary);
    color: white;
    box-shadow: inset 0 1px 0.5px rgba(255,255,255,0.25);
}
.btn-secondary .btn-core {
    background: rgba(0, 0, 0, 0.08);
    color: var(--color-text-main);
}
.btn-destructive .btn-core {
    background: rgba(255, 59, 48, 0.1);
    color: var(--color-negative);
}
/* Dark Mode Adjustments for Secondary/Destructive */
@media (prefers-color-scheme: dark) {
    .btn-secondary .btn-core {
        background: rgba(255, 255, 255, 0.1);
    }
}
/* src/styles/system.css */
/* System Header */
.system-toggle-wrapper {
  margin-top: 16px;
  width: 100%;
}
.system-subheader {
  margin-top: 24px;
  margin-bottom: 16px;
}
/* Data Management Spacing Fixes */
.section-label {
  font-size: 15px; /* text-md equivalent */
  font-weight: 600; /* font-semibold */
  color: var(--text-primary);
  margin-bottom: 8px; /* Standard gap */
}
/* Archival Engine Spacing */
.section-archival {
  margin-top: 24px;   /* Increased from implied default */
  margin-bottom: 4px; /* Tighter to description */
}
.archival-description {
  margin-top: 0;      /* Remove default p margin if any */
  margin-bottom: 12px; /* Tighten to button */
}
/* Universal Export Crowding */
.section-export {
  margin-top: 32px; /* Significant gap (approx 1.5rem+) */
}
/* Button Row Layout */
.button-row {
  display: flex;
  gap: 12px; /* 12px gap between buttons */
  width: 100%;
  margin-top: 16px; /* Prevent collision with Export section */
}
/* Storage Meter Track Override */
.storage-track {
  width: 100%;
  height: 8px;
  background-color: var(--separator);
  opacity: 0.2; /* To simulate bg-separator/20 */
  border-radius: 9999px;
  overflow: hidden;
}
/* Note: The 'opacity' above affects children if not careful.
   Better to use rgba or variable if possible.
   However, since we are constrained to vanilla CSS and existing variables:
*/
.storage-track-container {
    width: 100%;
    height: 8px;
    background-color: rgba(60, 60, 67, 0.1); /* Light mode separator approx */
    border-radius: 9999px;
    overflow: hidden;
}
@media (prefers-color-scheme: dark) {
    .storage-track-container {
        background-color: rgba(84, 84, 88, 0.25); /* Dark mode separator approx */
    }
}
/* src/styles/metric_builder.css */
/* --- Modal Header --- */
.metric-modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  border-bottom: 1px solid var(--separator);
  background-color: var(--bg-color);
  position: sticky;
  top: 0;
  z-index: 10;
  /* Match Main Card Radius */
  border-top-left-radius: var(--radius-card);
  border-top-right-radius: var(--radius-card);
}
.metric-modal-title {
  font-size: 20px;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
}
.btn-cancel-text {
  color: #8E8E93; /* Neutral gray */
  font-weight: 500;
  font-size: 16px;
  background: none;
  border: none;
  padding: 8px 16px;
  cursor: pointer;
  transition: color 0.2s;
}
.btn-cancel-text:hover {
  color: var(--text-primary);
}
.btn-cancel-text:active {
  opacity: 0.7;
}
/* --- Form Grid & Layout --- */
.form-row {
  display: flex;
  gap: 16px;
  width: 100%;
}
.form-group {
  flex: 1;
  display: flex;
  flex-direction: column;
}
/* Vertical alignment for Color Input to match Select */
.form-group-color {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-end; /* Aligns input to bottom */
}
/* --- Inputs Polish --- */
.input-standard {
  height: 44px; /* Standardize height */
  width: 100%;
  padding: 0 12px;
  border-radius: 10px; /* Consistent radius */
  border: 1px solid var(--separator);
  background-color: transparent;
  color: var(--text-primary);
  font-size: 16px; /* Legible size */
  font-weight: 500;
  outline: none;
  appearance: none; /* Remove native UI for selects */
  transition: border-color 0.2s;
}
.input-standard:focus {
  border-color: var(--blue);
}
/* Special handling for Color Input to match height */
.input-color {
  height: 44px !important;
  width: 100%;
  padding: 2px 4px; /* Padding for inner color swatch */
  border-radius: 10px;
  border: 1px solid var(--separator);
  background-color: transparent;
  cursor: pointer;
}
/* --- Labels --- */
.label-standard {
  font-size: 11px;
  font-weight: 700;
  color: var(--text-secondary);
  text-transform: uppercase;
  margin-bottom: 8px; /* Consistent gap */
  letter-spacing: 0.5px;
}
/* --- Divider Section --- */
.section-divider {
  margin-top: 24px;
  margin-bottom: 8px; /* Tighter gap */
  padding-bottom: 8px;
  border-bottom: 1px solid rgba(0,0,0,0.05); /* Subtle line */
  font-size: 11px;
  font-weight: 700;
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}
/* Dark mode adjust for divider */
@media (prefers-color-scheme: dark) {
  .section-divider {
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }
}
/* --- Content Wrapper --- */
.modal-content-scroll {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 16px; /* Vertical rhythm */
  overflow-y: auto;
  flex: 1;
}
.modal-footer {
  padding: 16px;
  border-top: 1px solid var(--separator);
  background-color: var(--bg-color);
  /* Match Main Card Radius */
  border-bottom-left-radius: var(--radius-card);
  border-bottom-right-radius: var(--radius-card);
}
.visualization-section {
  display: flex;
  flex-direction: column;
  gap: 0; /* Tight gap */
}
.widget-helper-text {
  font-size: 11px;
  color: var(--text-secondary);
  margin-top: 8px;
  line-height: 1.4;
}
*,
*::before,
*::after {
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}
html, body {
  margin: 0;
  padding: 0;
  width: 100%;
  background: var(--bg-color);
  color: var(--text-primary);
  font-family: var(--font-system);

  /* Liquid Native Scroll Rule */
  overflow-y: auto;
  overscroll-behavior-y: none;
}
#root {
  min-height: 100dvh;
  width: 100%;
}
.safe-bottom {
  padding-bottom: var(--safe-bottom);
}
/* --- Layout Utils --- */
.hidden { display: none !important; }
.flex { display: flex; }
.flex-col { flex-direction: column; }
.items-center { align-items: center; }
.justify-between { justify-content: space-between; }
.justify-center { justify-content: center; }
.gap-1 { gap: 4px; }
.gap-2 { gap: 8px; }
.gap-3 { gap: 12px; }
.gap-4 { gap: 16px; }
.p-4 { padding: 16px; }
.w-full { width: 100%; }
.h-full { height: 100%; }
.relative { position: relative; }
/* --- Typography Utils --- */
.text-xs { font-size: 11px; }
.text-sm { font-size: 13px; }
.text-md { font-size: 15px; }
.text-lg { font-size: 17px; }
.text-xl { font-size: 20px; }
.text-3xl { font-size: 34px; letter-spacing: -0.5px; }
.font-bold { font-weight: 700; }
.font-medium { font-weight: 500; }
.uppercase { text-transform: uppercase; }
.tracking-wide { letter-spacing: 0.5px; }
/* --- Color Utilities (Expanded) --- */
.text-primary { color: var(--text-primary); }
.text-secondary { color: var(--text-secondary); }
.text-blue { color: var(--blue); }
.text-green { color: var(--green); }
.text-red { color: var(--red); }
.text-orange { color: var(--orange); }
/* Background helpers */
.bg-blue { background-color: var(--blue); }
.bg-green { background-color: var(--green); }
.bg-red { background-color: var(--red); }
.bg-orange { background-color: var(--orange); }
/* --- Component Classes --- */
.card {
  background-color: var(--card-bg);
  border-radius: var(--radius-card);
  padding: 20px;
  box-shadow: var(--shadow-card);
  margin-bottom: 16px;
}
/* Liquid Glass Implementation */
.glass {
  background: var(--glass-bg);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-card);
  padding: 20px;
  margin-bottom: 16px;
  box-shadow: var(--shadow-glass);
}
.dashed-border {
  border: 2px dashed var(--separator);
}
.widget-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 16px;
}
/* Global Button Styles: REMOVED - Replaced by buttons.css */
/* --- Form Toggles (Checkbox) --- */
.toggle-checkbox {
  appearance: none;
  -webkit-appearance: none;
  background-color: white;
  border-radius: 9999px;
  cursor: pointer;
  height: 1.25rem;
  width: 1.25rem;
  transition: transform 0.2s ease-in-out;
  transform: translateX(0);
}
.toggle-checkbox:checked {
  transform: translateX(100%);
  border-color: white;
}
.toggle-label {
  width: 2.5rem;
  height: 1.5rem;
  background-color: var(--separator);
  border-radius: 9999px;
  position: relative;
  transition: background-color 0.2s ease-in-out;
}
input:checked + .toggle-label {
  background-color: var(--blue);
}
</style></head>
  <body>
    <div id="root"><div class="flex flex-col min-h-screen w-full bg-bg-color font-system text-primary relative"><main class="flex-1 w-full layout-padding"><div class="layout-padding fade-in"><div class="view-header-stack"><div class="text-xs font-bold text-secondary uppercase tracking-wide">Saturday, January 24</div><div class="flex justify-between items-end"><h1 class="text-3xl font-extrabold tracking-tight text-primary">Hi, Captain.</h1></div></div><div class="layout-content"><div class="glass p-8 flex flex-col items-center justify-center text-center gap-3 border-2 border-dashed border-separator border-opacity-50"><div class="text-4xl opacity-50 mb-2">⊕</div><div class="flex flex-col gap-1"><div class="text-lg font-bold text-primary">Welcome to ORBIT</div><div class="text-sm text-secondary max-w-[200px] mx-auto leading-relaxed">Your dashboard is empty. Configure your first metric to start tracking.</div></div><button type="button" class="btn-orbit btn-primary  mt-4" aria-label="Launch Setup"><div class="btn-core">Launch Setup</div></button></div><div class="fixed inset-0 z-[200] flex items-center justify-center bg-black/50 p-4 animate-fade-in"><div class="w-full max-w-lg h-[600px] relative"><button class="absolute -top-10 right-0 text-white font-bold">Close</button><div class="glass w-full max-w-lg flex flex-col bg-bg-color max-h-[90vh] overflow-hidden !p-0"><div class="metric-modal-header"><h2 class="metric-modal-title">Define your first 3 metrics</h2><div class="text-xs text-secondary font-bold">Step 1/4</div></div><div class="modal-content-scroll"><div class="flex flex-col gap-4 animate-fade-in"><div><label class="label-standard block">METRIC NAME</label><input placeholder="e.g. Focus Time" class="input-standard" type="text" value=""></div><div class="form-row"><div class="form-group"><label class="label-standard block">Type</label><select class="input-standard"><option value="boolean">Yes/No (Habit)</option><option value="number">Number (Count)</option></select></div><div class="form-group-color"><label class="label-standard block">Color</label><input class="input-color" type="color" value="#007AFF"></div></div><div class="visualization-section"><div class="section-divider">Visualization</div><div class="widget-helper-text">Heatmap tracking for daily habits and completion.</div></div></div></div><div class="modal-footer flex flex-col gap-3"><div class="flex gap-3"><button type="button" class="btn-orbit btn-secondary  flex-1 opacity-0 pointer-events-none" disabled="" aria-label="Back"><div class="btn-core">Back</div></button><button type="button" class="btn-orbit btn-primary  flex-1" aria-label="Next"><div class="btn-core">Next</div></button></div><button class="text-xs text-secondary underline hover:text-primary mx-auto pb-1">Skip Setup</button></div></div></div></div><div class="glass p-4 border-l-4 border-blue mb-4"><div class="flex flex-col gap-2"><div class="text-xs font-bold text-blue uppercase tracking-wider flex items-center gap-2"><span>✦</span> Horizon Agent</div><div class="text-secondary text-sm">I am your Horizon Agent. I work privately on your device to uncover patterns in your habits.</div></div></div></div></div></main><div class="tab-bar-container"><div class="glass tab-bar"><div class="tab-indicator" style="width: 64px; transform: translateX(0px);"></div><div class="tab-item active" role="button" aria-label="Horizon" aria-selected="true"><div class="relative z-10 active:scale-95 transition-transform duration-200"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class=""><rect x="3" y="3" width="7" height="7"></rect><rect x="14" y="3" width="7" height="7"></rect><rect x="14" y="14" width="7" height="7"></rect><rect x="3" y="14" width="7" height="7"></rect></svg></div></div><div class="tab-item " role="button" aria-label="Logger" aria-selected="false"><div class="relative z-10 active:scale-95 transition-transform duration-200"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class=""><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg></div></div><div class="tab-item " role="button" aria-label="Intel" aria-selected="false"><div class="relative z-10 active:scale-95 transition-transform duration-200"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class=""><circle cx="12" cy="12" r="2"></circle><path d="M16.24 7.76a6 6 0 0 1 0 8.49"></path><path d="M19.07 4.93a10 10 0 0 1 0 14.14"></path></svg></div></div><div class="tab-item " role="button" aria-label="System" aria-selected="false"><div class="relative z-10 active:scale-95 transition-transform duration-200"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class=""><path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.38a2 2 0 0 0-.73-2.73l-.15-.1a2 2 0 0 1-1-1.72v-.51a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"></path><circle cx="12" cy="12" r="3"></circle></svg></div></div></div></div></div></div>

    <script type="module" src="/src/main.jsx?t=1769217653642"></script>


</body></html>